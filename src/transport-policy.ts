export const DOM_ACTION_TRANSPORT_DISABLED_MESSAGE =
  "runtime DOM action recipes are disabled; browser and agent-browser are capture/bootstrap-only; use a code-owned schemaVersion 3 provider or schemaVersion 4 authenticated internal API contract";
