/**
 * Stable, inert implementation source for tests whose plugin runtime is
 * supplied by an in-memory harness rather than imported from a source module.
 */
export const providerPluginTestFixture = true;
