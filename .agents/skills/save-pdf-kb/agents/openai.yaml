interface:
  display_name: "Save PDF to KB"
  short_description: "Convert PDFs into auditable KB Markdown"
  default_prompt: "Use $save-pdf-kb to convert this PDF into a faithful Markdown bundle with structured text, OCR, local images, and provenance."
