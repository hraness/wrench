interface:
  display_name: "Query a Knowledge Base"
  short_description: "Load scoped context, then search with visible evidence"
  default_prompt: "Use $query-kb to load applicable repository-path context first, then retrieve the smallest useful set of notes with exact, QMD, graph, and Git evidence kept distinct."
