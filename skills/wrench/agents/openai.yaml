interface:
  display_name: "Wrench"
  short_description: "Capture, archive, and use bounded provider APIs"
  default_prompt: "Use $wrench to capture or archive this source, inspect a bounded capability, or save a private native article draft without publishing."
