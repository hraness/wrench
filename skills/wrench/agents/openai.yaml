interface:
  display_name: "wrench"
  short_description: "Clip sites and build safe direct-API capabilities"
  default_prompt: "Use $wrench to capture this site or turn one authenticated first-party exchange into a fail-closed code-owned CLI capability."
